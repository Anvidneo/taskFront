import React from 'react';
import ReactDOM from 'react-dom/client';
import RoutesFront from './routes/Routes';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <RoutesFront />
);